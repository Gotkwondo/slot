export type User = {
  memberId: string;
  loginId: string;
  name: string;
  activeStatus: string;
  permission: string;
  memo: string;
  lastLoginedAt: string;
  createdAt: string;
  createdBy: string;
  shopSlotCounts: number;
  placeSlotCounts: number;
};
